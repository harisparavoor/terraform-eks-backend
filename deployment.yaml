apiVersion: apps/v1
kind: Deployment
metadata:
  name: Backend_App
  labels:
    app: Backend_App
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Backend_App
  template:
    metadata:
      labels:
        app: Backend_App
    spec:
      containers:
        - name: Backend_App
          image: IMAGE_URI
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /healthz
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 60
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 3
